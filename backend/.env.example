PORT=3000
TRUSTED_ORIGINS=http://localhost:5173
NODE_ENV=development
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/helpdesk
BETTER_AUTH_SECRET=        # Required: run `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"` to generate
BETTER_AUTH_URL=http://localhost:3000  # Must be https in production
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=            # Required: set a strong password (min 12 characters)
